<h2>MY NUTRITION PAGE</h2>
<p class="white-text">Welcome{{user.username}}!</p>
{{!-- {{#if isLoggedIn}} --}}
<body>
<form id="searchform"> 
  <input type="search" id="query" name="q" placeholder="Search...">
  <button id="search-button">Search</button>
</form>
<h3 class="white-text">Healthy Recipes</h3>
  {{#each searchResults as |recipe|}}
    <div class="row">
      <div class="col s6">
        <div class="card">
          <div class="card-image">
            <img src={{recipe.image}}>
            <span class="card-title">{{recipe.title}}</span>
          </div>
          <div class="card-content">
           <p>{{{recipe.summary}}}</p>
          </div>
        </div>
      </div>
    </div>
  {{/each}}
{{!-- {{/if}} --}}

{{{body}}}
</body>